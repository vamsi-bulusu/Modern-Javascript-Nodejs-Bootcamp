const age = 27

const message = age >= 18 ? "You can vote" : "You can't vote"

console.log(message)

const team = ['vamsi', 'akash', 'dinakaran']

console.log(team.length <= 4 ? `Team size ${team.length}` : 'Too many people')